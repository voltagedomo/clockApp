// Stopwatch part by Alexander Alkin - seperate js to reduce amount of scrolling needed

// Variables
let recordedTime = 0;
let stoppedTime = 0;
let laps = 0;
startButton = document.getElementById("stopwatch-start");

// event listeners
startButton.addEventListener("click", stopwatch);

function stopwatch(startTime, lap) {}
